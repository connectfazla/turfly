name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  DATABASE_URL: postgresql://turf:turf@localhost:5432/turf_booking?schema=public
  NEXT_PUBLIC_SITE_URL: http://localhost:3000
  NEXT_PUBLIC_ROOT_DOMAIN: turfly.app
  NOTIFICATIONS_ENABLED: 'false'
  TZ: Asia/Dhaka
  # LOOPS_API_KEY intentionally unset: auth emails (verify/reset/invite) log
  # to the server console instead of sending — e2e/rbac.spec.ts and
  # e2e/accessibility-admin.spec.ts create their test accounts directly via
  # Prisma with a known password, so no real email ever needs to be
  # delivered in CI.

jobs:
  # typecheck, lint, unit tests — the coverage gate on lib/slots.ts and
  # lib/availability.ts is enforced by vitest.config.ts's 100% thresholds,
  # so `pnpm test:coverage` itself fails the job if coverage regresses.
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm test:coverage

  # build, migrate + seed a real Postgres, then Playwright: the live
  # concurrency proof, RBAC, and axe accessibility passes.
  e2e:
    runs-on: ubuntu-latest
    needs: checks
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: turf
          POSTGRES_PASSWORD: turf
          POSTGRES_DB: turf_booking
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U turf -d turf_booking"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm prisma migrate deploy
      - run: pnpm db:seed
      - run: pnpm build
      - run: pnpm e2e
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14

  # Lighthouse (performance/accessibility/best-practices/SEO) and a second
  # axe pass against the actual production build, with score thresholds —
  # BUILD_PLAN.md step 8. Runs after e2e so it only fires on an otherwise
  # green build.
  lighthouse:
    runs-on: ubuntu-latest
    needs: e2e
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: turf
          POSTGRES_PASSWORD: turf
          POSTGRES_DB: turf_booking
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U turf -d turf_booking"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm prisma migrate deploy
      - run: pnpm db:seed
      - run: pnpm build
      - run: pnpm start &
      - run: pnpm dlx wait-on http://localhost:3000
      - run: pnpm dlx @lhci/cli@0.14 autorun
